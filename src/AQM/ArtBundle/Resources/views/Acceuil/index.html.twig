{% extends "AQMArtBundle::layout.html.twig" %}

{% block body %}
    <div class="acceuil-div">
        <img class="logo_acceuil" src="{{ asset('bundles/aqmart/images/site/logo.png') }}" alt="logo_aqm">
        <p class="text-pres">
            <span class="important-word">Noël</span> approchant, vous recherchez des
            <span class="important-word">cadeaux uniques et originaux</span> fabriqués localement par deux amies d'enfance
        </p>
        <p class="text-pres">
            Une <span class="important-word">boutique éphémère</span> est ouverte à cette occasion
            <span class="important-word">3 fois par semaine</span>
        </p>
        <p class="text-pres">
                Sandra et Stéphanie vous accueillent pour vous faire découvrir leurs
            <span class="important-word">créations</span>,<br>
                notamment les
            <span class="important-word">vendredi soir</span>, où auront lieu des <span class="important-word">nocturnes</span>
                agrémentées de petits fours maison
        </p>

        {% if articles|length > 0 %}
            <div id="view_content" class="container">
                <div id="myCarousel" class="carousel slide" data-ride="carousel">
                    <!-- Indicators -->
                    <ol class="carousel-indicators">
                        {% for article in articles %}
                            <li data-target="#myCarousel" data-slide-to="{{ loop.index - 1 }}" {% if loop.index == 1 %} class="active" {% endif %}></li>
                        {% endfor %}
                    </ol>

                    <!-- Wrapper for slides -->
                    <div class="carousel-inner">
                        {% for article in articles %}
                            <div class="item {% if loop.index == 1 %} active {% endif %}">
                                <img class="carousel-img" src="{{ asset(article.image.webPath) }}" alt="{{ article.image.alt }}">
                                <div class="carousel-caption">
                                    <h3>{{ article.nom }}</h3>
                                    <p>{{ article.description|nl2br }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>

                    <!-- Left and right controls -->
                    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
                        <span class="glyphicon glyphicon-chevron-left"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="right carousel-control" href="#myCarousel" data-slide="next">
                        <span class="glyphicon glyphicon-chevron-right"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        {% endif %}

        <p style="margin-top: 40px" class="text-pres">
            Alors n'hésitez pas à leur rendre visite pour un moment de
            <span class="important-word">découverte</span> et de <span class="important-word">convivialité</span>
        </p>
        <img class="separateur_acceuil" src="{{ asset('bundles/aqmart/images/site/logo.png') }}" alt="logo_aqm">
        <p class="text-pres">
            Sachez que Stéphanie et Sandra se feront un plaisir de créer et personnaliser<br />
            des <span class="important-word">oeuvres à votre demande</span>
        </p>
    </div>
{% endblock %}
